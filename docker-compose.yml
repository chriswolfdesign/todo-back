version: "3.9"
services:
  postgres:
    container_name: todo-data
    restart: always
    image: postgres:latest
    volumes:
      - ./todo-data/data:/var/lib/postgresql/data
    env_file:
      - ./vars/todo-data.env
    ports:
      - "5432:5432"
